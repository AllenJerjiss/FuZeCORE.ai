# LLM-FuZe preprod bench template — conservative, stable settings

# Logs
LOG_DIR=/var/log/fuze-stack

# Naming
ALIAS_PREFIX=LLM-FuZe-
ALIAS_SUFFIX=-preprod

# Scope to one model tag (filled by generator)
# Example: INCLUDE_MODELS='^llama4:16x17b$'
INCLUDE_MODELS='^gemma3:27b-it-fp16$'

# Bench behavior (conservative)
FAST_MODE=1            # runtime opts; do not bake during search
EXHAUSTIVE=0           # stop after first working optimized variant per endpoint
BENCH_NUM_PREDICT=64
BENCH_NUM_CTX=4096
TEMPERATURE=0.0
TIMEOUT_GEN=90
VERBOSE=1

# Debugging / publishing (conservative)
DEBUG_BENCH=0          # lighter logs
PUBLISH_BEST=0         # do not bake best tag automatically in preprod

# Ollama service handling
CLEAN_START_TESTS=1
SKIP_TEST_UNITS=0
KEEP_FAILED_VARIANTS=0
GC_AFTER_RUN=1         # GC any created tags if applicable

# Candidate sweep control (conservative)
NG_PERCENT_SET="100 90 75 60 50 40 30 20 10"
